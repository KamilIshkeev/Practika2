<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="Practika2.Views.ManageCourseModulesView"
        Title="Управление модулями курса"
        Width="800" Height="600"
        WindowStartupLocation="CenterScreen"
        Background="#FCFCFC">
    
    <Grid Margin="24" RowDefinitions="Auto,*">
        <StackPanel Grid.Row="0" Spacing="16" Margin="0,0,0,16">
            <TextBlock Text="Модули и уроки курса" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#2E7D32"/>
            <Button x:Name="AddModuleButton" 
                    Content="➕ Добавить модуль" 
                    Classes="primary"
                    HorizontalAlignment="Left"
                    Click="OnAddModuleClick"/>
        </StackPanel>
        
        <ScrollViewer Grid.Row="1">
            <ItemsControl x:Name="ModulesItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Classes="courseCard" Margin="0,0,0,16">
                            <StackPanel Spacing="12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" 
                                               Text="{Binding Title}" 
                                               FontWeight="Bold"
                                               FontSize="18"/>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                        <Button Content="➕ Урок" 
                                                CommandParameter="{Binding}"
                                                Click="OnAddLessonClick"/>
                                        <Button Content="✏️" 
                                                CommandParameter="{Binding}"
                                                Click="OnEditModuleClick"/>
                                        <Button Content="🗑️" 
                                                CommandParameter="{Binding}"
                                                Click="OnDeleteModuleClick"/>
                                    </StackPanel>
                                </Grid>
                                <ItemsControl ItemsSource="{Binding Lessons}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#E0E0E0" BorderThickness="1" Padding="8" Margin="16,4,0,4">
                                                <TextBlock Text="{Binding Title}"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>



